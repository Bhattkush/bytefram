/**
 * API base URL configuration.
 *
 * Android emulator  → http://10.0.2.2:8000
 * iOS simulator     → http://localhost:8000
 * Physical device   → http://<YOUR_PC_LOCAL_IP>:8000
 *                     (find it with `ipconfig` on Windows, look for IPv4 under Wi-Fi)
 *
 * Change the value below to match your setup.
 */
export const API_BASE_URL = 'http://10.69.25.145:8000';
